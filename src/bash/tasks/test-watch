#!/bin/bash

[ -z "$TEST_RUNNER" ] && echo "No test runner found" && exit 0

task="$TEST_RUNNER-test-watch"
task_file=$(taskfile "$task")
[ -z "$task_file" ] && task="chokidar-test-watch"

npx task "$task"
