{
    "env": {
        "test": {
            "presets": [
                ["@babel/preset-env", { "targets": "defaults" }], 
                ["@babel/preset-react", { "runtime": "automatic" }]
            ]
        },
        "task": {
            "presets": [
                ["@babel/preset-react", { "runtime": "automatic" }]
            ]
        }
    }
}
