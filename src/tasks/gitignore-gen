#!/bin/bash
  
cat <<END >> $GIT_IGNORE
node_modules
/*cache
/*output
/build
/coverage
/dist
/metrics
/temp
/config.*
/itermocil.yml
/.vscode
.DS_Store
END

sort -u -o $GIT_IGNORE $GIT_IGNORE

git add "$GIT_IGNORE"
