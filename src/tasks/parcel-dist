#!/bin/bash
  
. $TASK_LIBRARY_SRC/load-task-vars

PARCEL_COMMAND="${1:-build}"

rm -rf "$DIST"
mkdir -p "$DIST"
cp -r "$PUBLIC" "$DIST/"

npx parcel "$PARCEL_COMMAND" \
    "$PUBLIC_INDEX_HTML" \
    --public-url "$DIST_PUBLIC_URL" \
    --dist-dir "$DIST"
