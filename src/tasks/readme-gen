#!/bin/bash
  
. $TASK_LIBRARY_SRC/load-task-vars

[ ! -f "$README_TEMPLATE" ] && exit 0

node \ 
    --experimental-loader @node-loader/babel \
    "$TASK_LIBRARY_LIB/readme-file-generator.js" "$README_TEMPLATE" > "$README"

npx doctoc --maxlevel=2 "$README"

git add "$README"
