#!/bin/bash
  
set -e

OPTIONS=()

if grep -q "@node-loader/babel" "./package.json"; then
    OPTIONS+=" --experimental-loader @node-loader/babel"
fi

BABEL_ENV="task" node ${OPTIONS[@]} "$README_GEN" > "$README"

npx task doctoc

git add "$README"
